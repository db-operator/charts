---
suite: Test webhook argument builder
templates:
  - webhook/*.yaml
tests:
  - it: logLevel=info
    template: webhook/deployment.yaml
    asserts:
      - equal:
          path: spec.template.spec.containers[0].args
          value:
            - --log-level=info
  - it: logLevel=debug
    template: webhook/deployment.yaml
    set:
      webhook:
        logLevel: debug
    asserts:
      - equal:
          path: spec.template.spec.containers[0].args
          value:
            - --log-level=debug
  - it: logLevel=error
    template: webhook/deployment.yaml
    set:
      webhook:
        logLevel: error
    asserts:
      - equal:
          path: spec.template.spec.containers[0].args
          value:
            - --log-level=error
